<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
       android:shape="rectangle">
    <gradient
        android:angle="270"
        android:centerY="50%"
        android:startColor="#ffffffff"
        android:endColor="#eecccccc"/>
</shape>

<!-->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
       android:shape="rectangle | oval | line | ring">
    <corners
       四角设定角度，优先级比下面四个低
       android:radius = "integer"

       左上右上，左下右下的角度
       android:topLeftRadius="integer"
       android:topRightRadius="integer"
       android:bottomLeftRadius="integer"
       android:bottomRightRadius="integer"

    这个标签与solid互斥，这个标签表示渐变效果
    <gradient
        渐变的角度，默认为0，其值必须为45的倍数，0表示从左到右，90表示从下到上
        android:angle="integer"

        渐变中心横纵坐标 可以"50%"
        android:centerX="integer"
        android:centerY="integer"

        渐变中间颜色
        android:centerColor="integer"

        渐变半径，仅当type="radial"有效
        android:gradientRadius="integer"

        渐变的起始颜色
        android:startColor="color"

        渐变的结束颜色
        android:endColor="color"

        渐变的类别，linear线性、radial径向渐变、sweep扫描线渐变默认线性
        android:type="liner | radial | sweep"

        一般为false，当Drawable作为StateListDrawable使使用true
        android:userLevel="true | false" />
     <padding
        内部空白
        android:left="integer"
        android:top="integer"
        android:right="integer"
        android:bottom="integer"/>
     <size
        android:width="integer"
        android:height="integer"/>

     填充颜色
     <solid
        android:color="color/>

     描边
     <stroke
        描边的宽度，越大则shape的边缘线就会看起来越粗
        android:width="integer"

        描边的颜色
        android:color="color"

        组成虚线的线段的宽度
        android:dashWidth="integer"

        组成虚线的线段之间的间隔，间隔越大则虚线看起来空隙越大
        android:dashGap="integer"/>
</shape>
<-->
